import 'package:flutter/material.dart';
import 'package:score_system/model/activity.dart';

class ActivityComponent extends StatelessWidget {

  final Activity _activity;

  ActivityComponent(this._activity);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

  Widget _buildActivityComponent(Activity activity) {
    Widget rez = Container();
    
    return rez;
  }

}